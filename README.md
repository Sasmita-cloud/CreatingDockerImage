# CreatingDockerImage
Basic CI Pipeline: Set up a GitHub Actions workflow for any simple Java or Python project that automatically runs tests and builds a Docker image on every push to main branch. Push intentionally broken code and verify the pipeline fails and notifies you.
